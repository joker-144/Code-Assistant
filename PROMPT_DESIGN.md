# Prompt 模板设计思路

> **更新说明**：本版从"纯代码生成器"扩展到"代码助手"全场景，新增代码理解、重构建议、Bug诊断等 prompt 模板。

## 设计原则

DevAgent 的所有 prompt 模板遵循 **CARE** 原则：

- **C**ontext（上下文）：告诉模型它在什么场景、扮演什么角色
- **A**ction（行动）：明确要做什么，不做什么
- **R**estriction（约束）：格式要求、禁止事项
- **E**xample（示例）：输出格式的 JSON Schema

> **两种模式的核心区别**：代码生成器的 prompt 重"创造"（从零写代码），代码助手的 prompt 重"分析"（理解已有代码）

---

## 全景图：所有 Prompt 模板

```
prompts/
├── planner.txt          # 大脑规划（通用）
├── code_gen.txt         # 代码生成（代码生成器）
├── code_fix.txt         # 代码修复（代码生成器）
├── code_explain.txt     # 🆕 代码解释（代码助手）
├── code_refactor.txt    # 🆕 重构建议（代码助手）
├── bug_diagnose.txt     # 🆕 Bug诊断（代码助手）
├── review.txt           # 审查（通用，Qwen初审）
├── arbitrate.txt        # 仲裁（通用，DeepSeek终审）
├── docs.txt             # 文档生成（通用）
└── tech_qa.txt          # 🆕 技术问答（代码助手）
```

---

## 模板分类

### 1. 大脑 Prompt（Planner）

**目标**：DeepSeek-V4 分析需求，输出结构化执行计划

**设计要点**：
- 明确角色边界：你只规划，不写代码
- 告诉它有哪些可用 worker（glm/qwen/brain）及各自能力
- 严格的 JSON 输出约束
- 规划原则清单（先分析后编码、小步验证、接口优先...）

**核心 prompt 结构**：
```
系统角色定位
  ↓
可用资源说明（worker 类型和能力）
  ↓
用户需求 + 项目上下文
  ↓
输出格式（JSON Schema）
  ↓
规划原则（约束条件）
  ↓
最后强调：只输出 JSON
```

**关键技巧**：
- "只输出 JSON" 在 prompt 头尾各说一遍
- 用 JSON 注释格式作为示例（不是真正注释，是 `"worker": "brain|glm|qwen"` 这种说明性写法）
- 给出 `depends_on` 让大脑自己编排执行顺序

### 2. 代码生成 Prompt（GLMWorker）

**目标**：GLM-4 拿到规格，输出高质量代码

**设计要点**：
- 详细的编码规范（类型提示、docstring、错误处理、PEP 8）
- 提供已有文件上下文（避免重复造轮子/冲突）
- 约束条件可定制（框架、库、风格）
- 输出格式：直接给代码，不要用 markdown 包裹

**核心 prompt 结构**：
```
角色定位（资深 X 语言工程师）
  ↓
编码规范（7 条硬性要求）
  ↓
项目上下文 + 已有文件
  ↓
具体任务 + 约束
  ↓
输出格式
```

**关键技巧**：
- 提供已有文件作为上下文能大幅减少幻觉
- `_extract_code()` 做代码块提取，兼容有无 markdown 包裹的情况
- temperature 设 0.3：介于创造性（0.7）和确定性（0.1）之间

### 3. 审查 Prompt（QwenWorker → DeepSeek Arbitrator）

**双层审查设计**：

```
第一层：Qwen 初审（快、便宜）
  ├─ 5 个维度的结构化评分
  ├─ 问题清单（severity + line + message）
  └─ 总体评价

第二层：DeepSeek 终审（仅关键任务）
  ├─ 验证 Qwen 审查结果
  ├─ 决定：pass / retry / retry_with_feedback
  └─ 给出具体修复指令
```

**为什么双层**：
- Qwen 审一次很便宜（¥0.02），DeepSeek 审一次贵（¥0.2）
- 90% 的代码 Qwen 初审就能发现问题
- DeepSeek 只在 Qwen 发现问题时才介入终审
- 这是成本优化，不是质量妥协

### 4. 仲裁 Prompt（Arbitrator）

**目标**：DeepSeek 决定执行结果的命运

**三种判决**：
- `pass`：合格，进入下一步
- `retry`：严重问题，完全重写
- `retry_with_feedback`：小问题，带着反馈修改

**设计要点**：
- 给出明确的审查标准清单
- 评分 1-10 作为量化参考
- 反馈要具体可操作（不是"写得不好"，是"第 42 行缺少输入校验"）

---

### 5. 代码解释 Prompt（Code Explainer）🆕

**目标**：帮助用户理解已有代码，而非生成新代码

**设计要点**：
- 角色定位：代码翻译官，把复杂代码翻译成人话
- 分层输出策略：一句话总结 → 逐段解读 → 关键决策点 → 注意事项
- 必须读取实际代码（通过工具层），不能凭训练数据猜测
- 引用具体行号

**核心 prompt 结构**：
```
角色定位（资深代码解读专家，擅长把复杂逻辑讲清楚）
  ↓
项目上下文（框架={}，语言={}，依赖={}）
  ↓
目标代码（完整代码内容 + 相关文件）
  ↓
任务（解释代码的用途、逻辑流程、设计意图）
  ↓
约束
  - 先用一句话概括做什么
  - 再逐段解释关键逻辑
  - 标注设计决策点（为什么这么做）
  - 如有潜在问题要明确指出
  - 对新人友好但不过度简化技术细节
  ↓
输出格式（Markdown，含行号引用）
```

**关键技巧**：
- 提供相关文件上下文（import链）能大幅提升解释的准确性
- temperature 设 0.1：解释代码需要确定性，不能"猜"
- 行号引用用 `[L42-L68]` 格式，让用户能快速定位

### 6. 重构分析 Prompt（Refactoring）🆕

**目标**：分析代码质量，给出可执行的重构建议

**设计要点**：
- 5个维度扫描：代码坏味道、设计模式机会、可测试性、性能、安全
- 给出严重程度分级（🔴严重 / 🟡警告 / 🔵建议）
- 每个建议都要有"修改前 → 修改后"对比
- 预估每个修改的影响范围和风险

**核心 prompt 结构**：
```
角色定位（你是资深架构师兼代码审查专家）
  ↓
分析标准：
  1. 代码坏味道（长函数>20行、重复代码、深度嵌套>3层、God Class）
  2. 设计模式（哪里该用Factory/Strategy/Observer但没用）
  3. 可测试性（依赖注入缺失、硬编码、静态方法滥用）
  4. 性能（N+1查询、多余循环、缓存缺失）
  5. 安全（注入、越权、信息泄露）
  ↓
目标代码（完整代码 + 相关依赖）
  ↓
输出：分级问题清单 + 具体修改方案 + 影响评估
```

**关键技巧**：
- 给出具体的行号和修改前后对比，而不是泛泛的"需要重构"
- 严重程度分级让用户能优先处理最关键的问题
- temperature: 0.2（分析需要严谨但允许一定灵活性）

### 7. Bug 诊断 Prompt（Bug Diagnosis）🆕

**目标**：根据现象定位根因并给出修复方案

**设计要点**：
- 输入包含：错误描述 + 错误堆栈 + 相关代码文件
- 诊断流程：现象 → 假设 → 代码验证 → 根因 → 修复
- 区分"症状"和"根因"（不能把表面现象当原因）
- 给出复现条件

**核心 prompt 结构**：
```
角色定位（你是资深调试专家，善于从错误信息反推根因）
  ↓
错误信息（{error_description} + {traceback}）
  ↓
相关代码（自动从 workspace 搜索并注入）
  ↓
分析要求：
  1. 先区分症状和根因（根因是真实原因，症状是表面表现）
  2. 逐层分析调用链，追溯到最底层出错位置
  3. 找出触发条件（什么情况下会触发这个Bug）
  4. 给出修复方案（修改前 vs 修改后代码对比）
  5. 评估修复风险（是否影响其他功能）
  ↓
输出格式（诊断报告：根因 + 触发条件 + 修复方案 + 风险）
```

**和其他 prompt 的关键区别**：
- 代码生成 prompt：正向推理（需求 → 代码）
- Bug 诊断 prompt：逆向推理（异常 → 根因 → 修复）
- temperature: 0.0（诊断必须 100% 基于代码证据，禁止猜测）

### 8. 技术问答 Prompt（Technical Q&A）🆕

**目标**：结合项目实际代码回答技术问题

**设计要点**：
- 第一步必读代码（不能凭训练数据记忆答）
- 回答必须引用具体文件和行号
- 区分"项目现状"和"理论最佳实践"

**核心 prompt 结构**：
```
角色定位（你是这个项目的技术专家，精通代码库的每个细节）
  ↓
项目上下文（自动从workspace注入的相关代码文件）
  ↓
用户问题
  ↓
回答要求：
  1. 基于实际代码回答，不是泛泛而谈
  2. 必须引用具体文件路径和行号
  3. 如果项目代码和最佳实践有差距，说明原因
  4. 给出改进建议（如果适用）
  ↓
输出格式（简洁回答 + 代码引用 + 建议）
```

**关键技巧**：
- prompt 中提供的项目代码上下文越多，回答越精准
- temperature: 0.1（技术问答需要准确性）
- 和普通 ChatGPT 的核心区别：回答基于实际代码证据，不是训练数据的泛泛之谈

---

## 共享 Prompt 设计模式

### 模式1：角色-边界-任务-约束-格式

所有 prompt 都遵循这个五段式结构：

```
[角色定义] 你是谁，擅长什么
    ↓
[能力边界] 你能做什么，不能做什么
    ↓
[具体任务] 上下文 + 要解决的问题
    ↓
[约束条件] 必须遵守的规则
    ↓
[输出格式] 输出应该长什么样
```

### 模式2：代码上下文的注入策略

让模型"看到"足够的项目代码是关键：

```
高相关代码（相似度>0.85）：完整注入，不截断
中相关代码（相似度 0.6-0.85）：注入函数签名 + 关键逻辑摘要
低相关代码（相似度<0.6）：仅注入文件路径，供模型按需请求读取
```

### 模式3：Temperature 选用指南

| 场景 | Temperature | 原因 |
|------|------------|------|
| 代码生成 | 0.3 | 平衡创造性和准确性 |
| 代码解释 | 0.1 | 需要确定性，不能"猜" |
| 代码审查 | 0.1 | 每次审查结果应该一致 |
| Bug 诊断 | 0.0 | 完全基于证据，禁止猜测 |
| 重构建议 | 0.2 | 需要一定创造力但要严谨 |
| 任务规划 | 0.2 | 需要精准但允许灵活编排 |
| 技术问答 | 0.1 | 准确性优先 |

---

## 生产环境的 Prompt 优化

当你有足够的使用数据后：

```python
# 1. 收集反馈数据
# 记录：哪个 prompt 版本 → 执行结果 → 人工评分

# 2. A/B 测试不同 prompt 变体
# prompts/code_gen_v1.txt vs prompts/code_gen_v2.txt

# 3. 动态 prompt 选择
# 根据任务复杂度、语言、框架自动选择最优 prompt 模板
```

---

## 反模式（不要这样做）

❌ **不要**在 prompt 里写 "You are an expert in everything"
→ 角色要具体，边界要清晰

❌ **不要**说 "do your best" 或 "try hard"
→ 给具体的评估标准和操作指令

❌ **不要**在代码生成 prompt 里说 "write good code"
→ 用 7 条硬性规范替代 vague 要求

❌ **不要**在 prompt 结尾说 "let me know if you have questions"
→ Agent 不会问你问题，它必须自己做决定

❌ **不要**在代码解释 prompt 里让模型凭记忆回答
→ 必须要求它先读取代码再回答
